{
  "name": "edullm-admin-backend",
  "version": "1.0.0",
  "description": "Admin microservice for eduLLM",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx prisma generate && nodemon server.js",
    "prisma:pull": "prisma db pull",
    "prisma:generate": "prisma generate",
    "docker:dev": "npx prisma generate && nodemon --legacy-watch server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "winston": "^3.11.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prisma": "^5.8.0"
  }
}
